<div class="modal fade" id="modalCrearGrupoCostoEnvio" tabindex="-1" role="dialog" aria-labelledby="largeModalLabel" aria-hidden="true">
    <div class="modal-dialog">
        <div class="modal-content">
            <div class="modal-header">
                <button type="button" class="close" data-dismiss="modal"><span aria-hidden="true">×</span><span class="sr-only">Close</span></button>
                <h4 class="modal-title" id="largeModalLabel"><i class="fa fa-plus-circle"></i> Crear configuración de Costo de Envío</h4>
            </div>
            <div class="modal-body">
                <form id="addConfCostoEnvio" method="post" action="{{ path('public_costoenvio_agregarcostoenvio') }}">
                    <div hidden id="error" style="color: red;">Complete correctamente los datos</div>
                    <div class="row">
                        <div class="col-md-4">
                            <div class="form-group">
                                <label>Costo ($):</label>
                                <input id="costoNumero" name="costoEnvio" type="number" class="form-control" placeholder="$ 0.00">
                                <p class="help-block">Costo del envío para esta configuración</p>
                            </div>
                        </div>
                        <div class="col-md-8">
                            <div class="checkbox">
                                <input id="esGratis" type="checkbox" name="costoGratis" value="1" style="margin-left: 0">
                                <label style="padding-left: 19px;">Esta configuración de envío será sin costo (Gratis)</label>
                            </div>
                        </div>
                    </div>
                    <div class="row mt20" id="containerDireccion">
                        <div class="col-md-4">
                            <div class="form-group">
                                <label>Provincia</label>
                                <select required="required" class="form-control" required="required" name="provincia" id="provinciaAdicionar">
                                    <option value="">Seleccione Provincia</option>
                                    {% for provincia in provincias %}
                                        <option value="{{ provincia.id }}">{{ provincia.nombre }}</option>
                                    {% endfor %}
                                </select>
                            </div>
                        </div>
                        <div class="col-md-4">
                            <div class="form-group">
                                <label>Ciudad</label>
                                <select required="required" class="form-control" required="required" name="ciudad" id="ciudadAdicionar">

                                </select>
                            </div>
                        </div>
                        {#<div class="col-md-4">#}
                            {#<a id="adicionarCosto" class="btn btn-success mt30"><i class="fa fa-plus-circle"></i> Adicionar</a>#}
                        {#</div>#}

                    </div>

                    <div class="row mt20">
                        <div class="col-xs-12">
                            <p class="help-block">Puedes aplicar también filtros de peso y dimensiones, para aplicar un costo determinado.
                            Si quieres que se aplique a todo el país, o a una región diferente a la seleccionada arriba, guarda esta configuración primero
                            y crea otra con los parámetros que necesitas.</p>

                            <p class="help-block">Los valores de Peso, Ancho, Alto y Profundidad, constituyen el valor tope a tener en cuenta para aplicar
                            el costo de envío indicado en esta configuración.</p>
                        </div>
                    </div>

                    <div class="row mt20" id="containerPeso">
                        <div class="col-md-3">
                            <div class="form-group">
                                <label for="peso">Peso (Kg)</label>
                                <input type="number" id="peso" name="peso" class="form-control" >
                            </div>
                        </div>
                        <div class="col-md-3">
                            <div class="form-group">
                                <label for="ancho">Ancho (cm)</label>
                                <input type="number" id="ancho" name="ancho" class="form-control" >
                            </div>
                        </div>
                        <div class="col-md-3">
                            <div class="form-group">
                                <label for="alto">Alto (cm)</label>
                                <input type="number" id="alto" name="alto" class="form-control" >
                            </div>
                        </div>
                        <div class="col-md-3">
                            <div class="form-group">
                                <label for="profundidad">Profundidad (cm)</label>
                                <input type="number" id="profundidad" name="profundidad" class="form-control" >
                            </div>
                        </div>

                    </div>
                    {#<div class="row">#}
                        {#<div class="col-xs-12" id="gruposC">#}
                            {#<h4 id="tituloGruposEnvio"></h4>#}
                            {#{% for g in gruposEnvios %}#}
                                {#<p>{{ g.provinciaid.nombre }}{{ g.ciudadid!=null?" > "~g.ciudadid.ciudadNombre:"" }}</p>#}
                            {#{% endfor %}#}
                        {#</div>#}
                    {#</div>#}
                </form>
            </div>
            <div class="modal-footer">
                <button type="button" class="btn btn-rw btn-default" data-dismiss="modal">Cancelar</button>
                <button  id="btnGuardarCambiosConfCostoEnvio"type="button" class="btn btn-primary" >Aceptar</button>
            </div>
        </div><!-- /modal content -->
    </div><!-- /modal dialog -->
</div><!-- /modal holder -->
